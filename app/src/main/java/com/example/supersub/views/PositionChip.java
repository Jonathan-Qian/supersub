package com.example.supersub.views;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

import androidx.annotation.Nullable;

public class PositionChip extends View {
    public PositionChip(Context context) {
        super(context);
    }

    public PositionChip(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public PositionChip(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public PositionChip(Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
